ID: project1
_schema-version: "3.1"
version: 1.0.0
description: "Generated by the SAP Web IDE Multitarget Application Generator"
parameters:
  enable-parallel-deployments: true

modules:
  - name: ui5app
    type: html5
    path: webapp
    parameters:
      buildpack: sap-ui5
    build-parameters:
      build-result: dist
      builder: grunt
      supported-platforms: []
      build-timeout: 15
    requires:
      - name: destination-service

resources:
  - name: destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite

  - name: uaa-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application

  - name: html5-apps-repo
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
